// tiny test program for PA1
/*
 * Multiline comments here
 * **** //
 *
 */
class PA1Test {
	int a;
	int[] b;
	public String[] strs;
	private void m;

	private Array[] do(int a, String b, void c) {
		this.x[4] = -(this.x[2] + this.z * x.y.z - 20 / 30);
		a_9__99b = !b;
		c = (a && b || d) && !e;
		f = x > 2 || y >= 3 || z < x + d <= m(2,3,4);
		boolean k = true || false;
	}

	public static void main(String[] args) {
		int n = 3;
		n.i = 4;
		n[4] = 7;
		m.n[2] = 1;
		n = 5 + n.m * n * n[n.m[3]];
		while(true) {
			String[] n = new String[10];
		}
	}

	void doSomething(int s) {
		return s + - - ! - 2 * -3;
	}
}

class Compiler {
	public static void main(String[] args) {
		if (args.length < 1) {
			System.exit(4);
		} else {
			Parser parser = new Parser();
			parser.parseProgram();
			y = x- -y;
		}
		if (args.a < 1) {
			e.printStackTrace();
			System.out.println(e);
			System.exit(4);
		}
	}
}